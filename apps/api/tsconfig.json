{
  "extends": "@genie/config/tsconfig/node.json",
  "compilerOptions": {
    "types": ["node"]
  },
  // `api/` is the Vercel shim — it imports the built dist/vercel.mjs which
  // doesn't exist at typecheck time, so it's checked via the bundle step instead.
  "include": ["src", "test", "scripts"]
}
